Bu klasör GEÇİCİ (tek kullanımlık) test dosyaları içindir.
- İçeriği git'e girmez (.gitignore).
- `npm run test` bu klasörü ÇALIŞTIRMAZ (vitest.config.ts `exclude`).
- Dosyalar SİLİNMEZ; aynı ada tekrar yazılır (CLAUDE.md §22).
